import { MissingStrategy } from './types';

export function handleMissing(match: string, path: string, defaultValue: string | undefined, strategy: MissingStrategy): string {
    // TODO: Implement missing handler
    return match;
}
